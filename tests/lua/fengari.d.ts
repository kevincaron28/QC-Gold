// fengari ships no types; the harness only uses a few functions.
declare module "fengari" {
  const fengari: any; // eslint-disable-line @typescript-eslint/no-explicit-any
  export default fengari;
}
